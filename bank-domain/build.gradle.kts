// bank-domain : 다른 모듈에 의존적이지 않은 가장 low level
// transaction, type 처리
dependencies {
    implementation("org.springframework.boot:spring-boot-starter-data-jpa")
    implementation("org.jetbrains.kotlin:kotlin-reflect")
}
